# Heavy Study Ensemble

Браузерный клиент, который запускает ансамбль бесплатных моделей через OpenRouter: три независимых прогона Gemini 2.0 Flash Thinking, Qwen 3 Max, DeepSeek R1 и Grok. Финальный ответ подтверждается арбитром на Gemini.

## Что умеет

- 📸 Захватывает задачи с камеры, галереи, буфера обмена или вручную введённого текста.
- 🔁 Запускает три прогона Flash, подключает Qwen, DeepSeek и Grok — все ответы сохраняют reasoning/Thinking.
- 🧑‍⚖️ Передаёт все решения арбитру, который выбирает итоговый ответ и показывает отчёт с уверенностью.
- 🗂️ Сохраняет результаты в локальную историю (IndexedDB), чтобы вернуться к ним позже.
- 🌗 Поддерживает светлую и тёмную тему, работает полностью в браузере без сборки.

## Как запустить по ссылке

1. Разместите содержимое папки `llm` на любом статическом хостинге с HTTPS (GitHub Pages, Netlify, Vercel, Cloudflare Pages и т.д.).
2. После деплоя страница будет доступна по ссылке вида:
   ```
   https://<ваш-домен>/<путь-до-приложения>/
   ```
3. Перейдите по этой ссылке из браузера (HTTPS обязателен, чтобы камера и буфер обмена работали корректно).
4. В открывшемся интерфейсе нажмите «Настроить OpenRouter», вставьте бесплатный API-ключ и сохраните его.
5. Теперь можно загружать задачи и запускать Heavy-ансамбль прямо по этой ссылке.

### Быстрый деплой на GitHub Pages

```bash
# Клонируйте репозиторий и перейдите в папку
git clone <your-repo-url>
cd llm

# Создайте новую ветку gh-pages и отправьте файлы
git checkout --orphan gh-pages
git add .
git commit -m "Deploy"
git push origin gh-pages --force
```

Затем в настройках GitHub включите Pages для ветки `gh-pages`. После публикации откройте ссылку `https://<username>.github.io/<repository-name>/`.

## Настройка OpenRouter

1. Получите бесплатный API-ключ на [https://openrouter.ai/keys](https://openrouter.ai/keys).
2. В интерфейсе нажмите «Настроить OpenRouter», вставьте ключ и сохраните его. Ключ хранится только в браузере.
3. При необходимости можно удалить ключ кнопкой «Удалить ключ».

## Файловая структура

```
llm/
├── index.html      # Основная страница
├── styles.css      # Стили и темизация
├── app.js          # UI-логика и интеграция с orchestrator
├── orchestrator.js # Вызовы OpenRouter и арбитр
├── manifest.json   # Дополнительные метаданные (опционально)
├── icons/          # Иконки интерфейса
└── README.md       # Это описание
```

## Ограничения

- Для использования камеры/буфера требуется HTTPS.
- Необходим ключ OpenRouter; без него запросы к моделям не выполняются.
- Grok Beta может быть недоступен, если закончилась бесплатная квота в OpenRouter.
- В отличие от PWA, офлайн-режим и сервис-воркер отключены — приложение рассчитано на работу через веб-ссылку.
